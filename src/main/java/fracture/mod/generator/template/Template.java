package fracture.mod.generator.template;

public @interface Template {
	// $$ start of template
	// _ delimiter
	// $ function name
	// (no symbol) parameter name
	// $_ sub-parameter (equivalent to parameter.subparameter in JSON)
}
